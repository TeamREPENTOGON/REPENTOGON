struct IsaacString { {{
    const char* Get() const {
        if (size < 16) {
            return text;
        } else {
            return *(char**)text;
        }
    }
}} 

    char text[16];
    uint32_t size; // 0x10
    uint32_t unk; // 0x14
};
